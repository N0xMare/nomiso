-- Graph relations (light MVP).

DEFINE TABLE IF NOT EXISTS mentions TYPE RELATION FROM memory TO entity SCHEMALESS;
DEFINE TABLE IF NOT EXISTS belongs_to TYPE RELATION FROM memory TO entity SCHEMALESS;
DEFINE TABLE IF NOT EXISTS supersedes_edge TYPE RELATION FROM memory TO memory SCHEMALESS;
